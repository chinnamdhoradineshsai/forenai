services:
  - type: web
    name: forenai-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "python backend/server.py"
    envVars:
      - key: PORT
        value: 8000
